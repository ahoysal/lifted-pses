#!/bin/bash
#SBATCH --job-name=parallel
#SBATCH --partition=gpu
#SBATCH --nodes=1
#SBATCH --ntasks=1
#SBATCH --cpus-per-task=8
#SBATCH --gres=gpu:1
#SBATCH --mem=16G
#SBATCH --time=16:00:00
#SBATCH --array=0-20
#SBATCH --output=results/%A/%j_%a.out
#SBATCH --error=results/%A/%j_%a.err

set -euo pipefail

mkdir -p results/out results/err

# -----------------------------------------------------------------------------
# Parameter Array Setup
# -----------------------------------------------------------------------------

# Format: "Dataset Model PSE"
CONFIGS=(
    "-d Tree -m MeanGuesser"
    "-d Tree -m Transformer"
    "-d Tree -m Transformer --HodgePE"
    "-d Tree -m Transformer --RWPELifted"
    "-d Tree -m Transformer --LapPE"
    "-d Tree -m Transformer --RWPE"
    "-d Tree -m GCN"

    "-d Cycles5 -m MeanGuesser"
    "-d Cycles5 -m Transformer"
    "-d Cycles5 -m Transformer --HodgePE"
    "-d Cycles5 -m Transformer --RWPELifted"
    "-d Cycles5 -m Transformer --LapPE"
    "-d Cycles5 -m Transformer --RWPE"
    "-d Cycles5 -m GCN"

    "-d Cycles10 -m MeanGuesser"
    "-d Cycles10 -m Transformer"
    "-d Cycles10 -m Transformer --HodgePE"
    "-d Cycles10 -m Transformer --RWPELifted"
    "-d Cycles10 -m Transformer --LapPE"
    "-d Cycles10 -m Transformer --RWPE"
    "-d Cycles10 -m GCN"
    
    # "-d ZINC -m Transformer --LapPE --RWPELifted"
    # "-d ZINC -m Transformer --HodgePE --RWPELifted"
)

# Extract the specific parameters for this array task ID
CURRENT_CONFIG="${CONFIGS[$SLURM_ARRAY_TASK_ID]}"

# -----------------------------------------------------------------------------
# Environment setup
# -----------------------------------------------------------------------------

PROJECT_ROOT="${PROJECT_ROOT:-/home/aagrim/pse}"

if [[ -f "${HOME}/.bashrc" ]]; then
    set +u
    source "${HOME}/.bashrc"
    set -u
fi

if [[ -f "${PROJECT_ROOT}/.venv/bin/activate" ]]; then
    set +u
    source "${PROJECT_ROOT}/.venv/bin/activate"
    set -u
    echo "Activated virtualenv at ${PROJECT_ROOT}/.venv"
elif [[ -f "${PROJECT_ROOT}/venv/bin/activate" ]]; then
    set +u
    source "${PROJECT_ROOT}/venv/bin/activate"
    set -u
    echo "Activated virtualenv at ${PROJECT_ROOT}/venv"
else
    echo "Warning: No virtualenv found. Using system Python."
fi

# -----------------------------------------------------------------------------
# Run
# -----------------------------------------------------------------------------

cd "${PROJECT_ROOT}"

echo "Running holygrail.py $CURRENT_CONFIG from $(pwd)"
echo "Using GPU: ${CUDA_VISIBLE_DEVICES:-none}"
echo "Python version: $(python3 --version)"
echo "PyTorch version: $(python3 -c 'import torch; print(torch.__version__)')"
echo "CUDA available: $(python3 -c 'import torch; print(torch.cuda.is_available())')"

if python3 -c 'import torch; exit(0 if torch.cuda.is_available() else 1)' 2>/dev/null; then
    echo "CUDA device: $(python3 -c 'import torch; print(torch.cuda.get_device_name(0))')"
fi
echo ""

# Pass the extracted array parameters directly into the script
python3 -u scripts/holygrail.py $CURRENT_CONFIG